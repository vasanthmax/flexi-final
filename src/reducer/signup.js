// const initState = {
//   authData: [],
// };

// const signupReducer = (state = initState, action) => {
//   switch (action.type) {
//     case 'SIGN_UP':
//       localStorage.setItem('profile', action.payload._id);
//       return { ...state, flip: action.payload._id };

//     default:
//       return initState;
//   }
// };

// export default signupReducer;
